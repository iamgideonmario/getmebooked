<%- include('../partials/header', { title: 'Payment Successful', user }) %>

<div class="alert alert-success rounded-4 shadow-sm">
  <h2 class="h4">Payment successful</h2>
  <p class="mb-0">Your booking has been confirmed.</p>
  <% if (bookingId) { %>
    <p class="small text-muted">Booking ID: <%= bookingId %></p>
  <% } %>
</div>

<div class="mt-4">
  <a class="btn btn-primary" href="/dashboard">View dashboard</a>
  <a class="btn btn-outline-secondary" href="/">Back to home</a>
</div>

<%- include('../partials/footer') %>